Cliente UDP

funcao pegarPortaOrigem {
    provavelmente vamos ter que usar shell, para pegar o
    número do processo, algo assim
    para por como porta de origem
}

funcao udp{

//itens do cabeçalho udp
mensagem=pegaDadosDaAplicacao
portaOrigem=pegarPortaOrigem()
portaDestino=portaDoServidorDaCamadaDeAplicacao
// usado para identificar o processo da camada de aplicação
comprimento do segmento UDP (incluindo cabeçalho)
checksum
enviarDadosParaCamadaFisica
}
